import AdminGrantBundleContent from "@/components/AdminGrantBundleContent";

export const metadata = {
    title: "Grant Bundle | PlayForges Admin",
    description: "Grant store bundles to users.",
};

export default function AdminGrantBundlePage() {
    return <AdminGrantBundleContent />;
}
